{% extends "proyecto/project_base" %}

{% block "working_path" %}
	{{ block.super }}
	<li class="active">{{ section_title }}</li>
{% endblock %}

{% block "content" %}

	<div class="panel panel-default">

		<div class="panel-body">

		<div class="row">
		                <div class="col-md-12">
		                    <h3>{{ section_title }}</h3>
				    <hr />

			<table class="table table-striped">
				<thead>
					<tr>  <th>Usuario</th> <th class="text-center">Hs Hombre diarias</th></tr>
				</thead>
				{% if dev_list|length == 0 %}
				<tbody>
					<tr class="danger">  <td colspan="3">Actualmente no hay desarrolladores</td> </tr>
				</tbody>
				{% else %}
				<tbody>
					{% for dev in dev_list %}
					<tr>
						<td><a href="{% url URL_NAMES.PERFIL_NAME dev.id %}">{{dev.user.user.first_name}} {{dev.user.user.last_name}}</a></td>
						<td class="text-center">{{dev.hs_hombre}}</td>
						<td class="text-right">

							<a href="{% url URL_NAMES.PROJECT_DEV_EDIT project.id dev.id %}">
							<button type="button" class="btn btn-default btn-sm">
								<span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
							</button>
							</a>
						</td>
					</tr>
					{% endfor %}
					<tr>
                        <td class="text-right"><b>Capacidad del Equipo:</b></td>
                        <td class="text-center">{{project.capacity}}</td>
                        <td></td>
                    </tr>
				</tbody>
			{% endif %}
			</table>

			<span class="step-links">
				{% if page_obj.has_previous %}
					<a href="?page={{ page_obj.previous_page_number }}"><span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span></a>
				{% endif %}

				<span class="current">
					Pagina {{ page_obj.number }} de {{ page_obj.paginator.num_pages }}
				</span>

				{% if page_obj.has_next %}
					<a href="?page={{ page_obj.next_page_number }}"><span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span></a>
				{% endif %}
			</span>

		</div>

		</div>

		</div>
	</div>




{% endblock %}